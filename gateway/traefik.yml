# Traefik static configuration
api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":8080"
  dashboard:
    address: ":8081"

providers:
  docker:
    exposedByDefault: false
    network: chatapp-network

log:
  level: INFO
